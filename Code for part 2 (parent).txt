// Code for part 2 (parent)

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h> // To be able to use usleep for the delay
 // Not sure if the counter is global or local between the parent and child process 
int main() {
    pid_t pid; 
    
    pid = fork(); 
        if (pid < 0){
            perror("fork() failed"); 
            exit(1);
        if (pid == 0){
            int counter_child = 0;            
            while(1){
                printf("Cycle number: %d", counter_child--);
                }
                usleep(50000); // Creates a delay of 0.5 secs

            }
        }
    }
    
    
    return 0;
}