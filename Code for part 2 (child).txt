// Code for part 2 (child)

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h> // To be able to use usleep for the delay
 
int main() {
    pid_t pid; 
    
    pid = fork(); 
        if (pid < 0){
            perror("fork() failed"); 
            exit(1);
        if (pid == 0){
            int counter_child = 0;
            while(1){
                printf("Child process counter: %d\n", counter_child++); 
                if (counter_child % 3 == 0){
                    printf("Cycle number: %d - %d is a multiple of 3", counter_child, counter_child); 
                }
                else{
                    prinf("Cycle number: %d", counter_child++);
                }
                counter_child++; 
                usleep(50000); // Creates a delay of 0.5 secs

            }
        }
    }
    
    
    return 0;
}